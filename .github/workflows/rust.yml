name: Rust

on:
  push:
    branches: [ dev-0.1 ]
  pull_request:
    branches: [ dev-0.1 ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: test all
      run: sh test.sh

